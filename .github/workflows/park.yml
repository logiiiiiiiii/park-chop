name: Triggered from Home Assistant

on:
  repository_dispatch:
    types: [home-assistant-trigger]

jobs:
  test:
    name: Setup
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-node@v4
        with:
          node-version: lts/*

      - name: Run Playwright test
        run: npx playwright test hilde.spec.ts
